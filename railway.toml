[build]
builder = "DOCKERFILE"
dockerfilePath = "legalpro-app/Dockerfile"

[deploy]
startCommand = "node server/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 10
healthcheckInterval = 30
restartPolicyType = "ALWAYS"
